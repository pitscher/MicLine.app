# MicLine.app — Decision Log

> Provenance: P1 interview 2026-07-06 · **P2 final product interview 2026-07-12**. Format per entry: decision — why — rejected alternatives. Flags: **[ASSUMED]** = Claude's call under interview rules (veto anytime) · **[OPEN]** = deferred, owner noted · **[PARKED]** = deliberately shelved · **[REJECTED]** = decided against · **[SUPERSEDED]** / **[AMENDED]** = changed by a later decision (pointer given). Unflagged = confirmed by the operator. Sections A–H are the P1 log with P2 annotations; sections P2-1…P2-11 are the P2 interview. The consolidated open-questions and scheduled-decisions registry is at the end.

## A — Audience & positioning (P1)

- **A1 · Live-mic model is the identity.** The line orders *people who speak* (walk-up, mic passed, or unmuted); "up next" prepares people mentally. Why: it's the wedge no incumbent owns and matches the name. Rejected: read-aloud-only model (Slido's home turf; falls out for free anyway).
- **A2 · Primary segment: in-room events, meetup-to-conference (≈30–500).** Classrooms tolerated, not marketed; livestreams out of design scope M1/M2 (no OBS/chat integrations). Why: each other segment drags hidden cost onto a solo operator. **[AMENDED P2 → T2-7]:** online/hybrid meetings are a fully supported usage context (URL join works in a video call); in-room stays the design center; the integration rejection is untouched.
- **A3 · One event = one room/session.** Parallel tracks = separate events; no umbrella/series concept. Design envelope ~500 concurrent participants, platform hard cap 1,000 (see G1).
- **A4 · Buyer = prosumer moderator, self-serve.** B2B = a bigger invite code sold manually via email. Rejected: teams/seats/org accounts, white-label.
- **A5 · Pillar order: the line → zero friction → privacy; simplicity is a principle, not copy.** Open source is not a marketing pillar: the running SaaS must not link/refer to the repo until the operator changes course.

## B — M1 core walkthrough (P1)

- **B1 · Landing = moderator pitch + participant rescue lane.** Hero (line pillar), 3-step how-it-works, create CTA, prominent join-code box, honest privacy block, visual mock; clean page, depth in sub-pages; no pricing page, no repo link. Rejected: participant-first landing; overloaded single-pager.
- **B2 · Join methods (M1): QR + short URL `/e/⟨CODE⟩` + landing code box,** all encoding the same 6-char code. Vanity slugs → M2B (STD-09); NFC etc. parked.
- **B3 · Zero-gate join.** Board visible instantly; no account/name/cookie-banner/CAPTCHA on entry; invisible Turnstile on first submission only; anonymous session cookie. Why: seconds between scan and board kill adoption in a dark room.
- **B4 · Moderator identity: durable-but-minimal.** Email + optional display name; recognized on return; auto-purge per G3b; self-service delete from M1. Rejected: truly ephemeral accounts; hashed-pseudonymous middle. [SUPERSEDED the initial "purged right after use" sketch during P1.]
- **B5 · Name modes per event: Nicknames (automatic, default) / Real names (required).** Name prompt before *first submission*, not on entry; upvotes (M2B) nameless in both modes. Rejected: per-question anonymity toggle; name verification.
- **B6 · Name-typo lifecycle: confirm step + leave-and-rejoin.** Leave resets the session (prior questions keep old attribution; vote dedupe resets — best-effort anyway; Turnstile re-runs). Rejected: mid-event rename; timed edit windows; moderator renames. **[P2 note → T1-2]:** leave-and-rejoin is also the accepted residual of the one-active-question rule.
- **B7 · Participant surface: full-line transparency.** Now speaking pinned, whole ordered line with positions, own-question status chips, position cues. Rejected: own-position-only view.
- **B8 · Projector board ships in M1, deliberately minimal.** One read-only route: QR + code corner, Now/Up-next huge, top of line, announcement area, fullscreen toggle; no settings. Appearance controls → M4. **[AMENDED P2 → T3-4, T8-8]:** the board route runs behind its own capability URL (never exposes event ID/join code) and can be hidden (MOD-06) and re-minted (MOD-07).
- **B9 · Event states on join:** lobby pre-start, live board, ended screen with join-another path, unknown/expired-code screen, event-full screen. Participant-facing recap page killed [PARKED PRK-03].
- **B10 · M1 line = Open line (FIFO) only.** Submit → appended to the public line; order = arrival. Domain model built two-mode-shaped from day one. **[AMENDED P2 → T1-1]:** one active waiting item per participant (LINE-08); position stability is explicitly an *open-line* guarantee (curated mode trades it for visible human ordering).
- **B11 · No upvotes in M1.** Votes can't influence a FIFO line; shipping a do-nothing button is worse than absence. Rejected: sentiment-only votes in M1.
- **B12 · No review mode in M1.** Pre-moderation becomes an M2B event option with consequence hints at creation. Semantics [was OPEN → **CLOSED P2 → T4-2**].
- **B13 · Moderator ground control stays in M1: skip any line item, transparently.** Public label "skipped", not "declined" (no content judgment; covers no-shows). **[AMENDED P2 → T3-1]:** skip removes the question *text* from all public surfaces; a stub (attribution + label) remains; the board never shows skipped items.
- **B14 · Undo: ~10 s single-level toast after answered and skip.** Rejected: history stack.
- **B15 · Withdraw own question pulled into M1.** Completes the participant lifecycle cheaply. **[P2 note → T1-4]:** slot frees instantly under LINE-08.
- **B16 · Settings freeze once live.** Editable until start; mid-event mode switches rejected as a state-machine minefield. Scheduling fields keep their ±24 h window. **[P2 note]:** live controls (announcements, intake close, hide board + its message) are deliberately *not* settings — B18 precedent.
- **B17 · Pre-join primer in M1, plain form.** Optional plain multiline text; per-event choice skippable vs required acknowledgment. Styling → M2B (STD-08, markdown-lite, no links — phishing vector). **[AMENDED P2 → T2-2, T2-3, T2-6]:** re-openable in-event from M2B; links inert everywhere; required mode gains a customizable confirmation statement.
- **B18 · Announcements are a live control, not a setting.** One current announcement, replaces previous, shown on board + participant surfaces; no history/scheduling. **[AMENDED P2 → T2-5]:** bounded display lifetime with uniform auto-expiry, clear-early action, self-dismissal cue.
- **B19 · Event scheduling model.** Create up to 48 h ahead; Scheduled until required start; required end; start/end adjustable within ±24 h of originally configured values; beyond: delete + recreate; auto-end at end_time + 24 h. Rejected: optional times with prefill; hard auto-end at end_time. **[AMENDED P2 → T8-5]:** unlimited reschedules within the anchored window (count cap rejected); rescheduling is rate-limited and never a gated act.
- **B20 · Creation form (M1):** title*, start*, end*, private description, event language, name mode, primer (+ acknowledgment mode), profanity filter. Under a minute to create. **[AMENDED P2 → T1-6, T2-6, T4-1]:** + min/max question length; + custom confirmation statement; (M2B: + pool visibility, upvotes, participant submission limit.)
- **B21 · Console (M1):** one phone-capable screen — header (title, share panel, live participant count, end button), the line (answered/skip + undo), announcement composer. My-events list: scheduled/active/past.
- **B22 · Post-event feedback funnel in M1, minimal.** Optional post-end feedback box → email to dedicated FEEDBACK_EMAIL (Wrangler secret in M1; admin setting in M2A).

## C — M2 stand-out features (P1)

- **C-verdicts:** keep: curated line, upvotes+pool sort, review mode, co-moderators, recap & export, rich announcements/primer, duplicate/templates, vanity slugs, speaker timer, branding (post-M4), TMS. Kill: participant recap page [PRK-03], topics/tags [PRK-01], live reactions [PRK-02]. Moved to M1: withdraw own question. Pre-killed set (polls/quizzes/word clouds, deck embed, livestream ingestion, SMS, white-label, series, public directory) accepted without objection.
- **C-rank (P1):** 1 curated · 2 upvotes · 3 review · 4 co-mods · 5 recap & export · 6 timer · 7 duplicate/templates · 8 rich announcements · 9 vanity · 10 branding · parallel TMS. **[SUPERSEDED P2 → T7-1 + T10-1]:** recap & export raised to rank 1 and moved into M2A; remaining force-rank unchanged within M2B.
- **C15-untangle:** Lingui = M1/F000; Lunaria loop = M1 tooling; rented TMS = M2B, replaces-or-feeds Lunaria decided at M2B kickoff. [OPEN → M2B kickoff]

## D — Admin interface (P1)

- **D1 · Admin identity managed exclusively Cloudflare-side.** ADMIN_EMAILS Wrangler secret grants the role at login; no in-product admin management; recovery = update the secret. Rejected: in-product admin management, one-time setup tokens.
- **D2 · Admin second auth layer.** **[AMENDED P2 → T8-1, half-closed]:** a dedicated mandatory wall on /admin is *decided*; only the mechanism (Cloudflare Access vs TOTP vs passkey) remains open for the tech interview, with the Cloudflare-account-reset requirement attached. Step-up confirmation on destructive actions confirmed regardless.
- **D3 · Oversight v1:** search events/moderators + detail, read-only god view of live events, force-end, soft-delete, ban moderator email, GDPR full delete. Global stats view: totals + anonymous aggregates. **[AMENDED P2 → T8-3, T8-7]:** god view shows live states (board hidden, intake closed); force-end and soft-delete gained defined communications.
- **D4 · Aggregation-at-deletion principle:** anonymous aggregates survive account/event deletion; everything identifying dies. Country from Cloudflare request geolocation, disclosed on the privacy page.
- **D5 · Abuse doctrine:** abusive participants = moderator's problem (skip, primer, filters, rate limits — no identity to ban); abusive moderators/events = admin's (ban email, end/delete). No in-product report button [PRK-05]; reports via contact email. **[P2 note]:** moderator toolkit now includes skip-stub, intake close, hide board.
- **D6 · Metrics wall (own counters, no third-party analytics):** events created/active/week, participants, questions, emails sent, Turnstile failures, rate-limit hits, storage growth; € projection arrives with the M5 cost model.
- **D7 · Platform controls:** localized service-notice banner + event-creation kill-switch as KV flags in M1 (wrangler CLI; runbook), admin UI wraps them in M2A. KV-flag pattern may generalize into the platform feature-flag mechanism — tech-interview topic. **[AMENDED P2 → T8-9]:** service-notice placement defined (always landing/auth/console; participant surfaces + board only when flagged as affecting live events).
- **D8 · Emergency controls split.** Two separate controls: **Emergency stop** (reversible) and **Delete all data** (GDPR/breach nuke: step-up auth + typed confirmation). D1 time-travel restores the global DB ≤30 days; per-event DO storage is unrecoverable — acceptable. Rejected: one bundled end-everything-and-wipe button. **[AMENDED P2 → T8-6]:** emergency stop became a *total stop* with typed confirmation, registration block, auto-export machinery, scheduled-event rules, job pause + grace, and emails E-4/E-4b/E-5.
- **D9 · Admin UI v2 wishlist** [PARKED PRK-08].

## E — Invite codes & entitlements (P1)

- **E1 · Free baseline (lockdown off): all shipped features free.** Limits: 100 participants/event, 1 active + 1 planned event; unlimited ended events. Entitlement seam wraps every gateable thing from day one.
- **E2 · Gateable dimensions:** never-gated = core Q&A mechanics; gateable = co-moderators, event branding, vanity slugs, limit raises, registration permission. Duration-based gating dropped. **[AMENDED P2 → T8-2]:** all P2 moderator tools (intake close, hide board, regenerate board link, pool settings/limits, decline reasons, follow-up flag, filters/search/bulk, export) join the never-gated set.
- **E3 · Invite code = a bundle** `{may_register? + capability grants + limit raises + validity}`; god-voucher = one-click preset. Voucher-design details recorded verbatim as M2A-kickoff input. [OPEN → M2A kickoff]
- **E4 · Lockdown gates *creation*, not *continuation*.** New registrations and new event creation require a code; already-created events run/start normally. Rejected: kick-out sketch; registration-only lockdown. **[AMENDED P2 → T8-3]:** duplicating an event is creation (gated); rescheduling a committed event is never gated.
- **E5 · Redemption: codes fill an account wallet.** Redeem at registration or anytime; grants apply to events created while active; metered grants decrement at creation; `max_redemptions` per code.
- **E6 · Two independent expiry clocks:** redeem-by (code) and grant validity (wallet). Defaults at M2A kickoff.
- **E7 · Revocation never touches a live event** — effect always at the next gated act.
- **E8 · Billing (M3-if-ever) = just another grantor** writing identical grant rows. **[P2 note → T8-4]:** drastic admin controls need a compensation & communication policy toward paying customers before billing ever ships (BIL-01 note).
- **E9 · Entitlement transparency:** account page always shows active grants, meters, validity.

## F — Design direction (P1, M4 seed)

- **F1 · Adjectives:** calm · clear · even-handed · trustworthy · quietly confident; playfulness only in participant micro-moments; console/admin all-business.
- **F2 · North-star reference: Luma.** Anti-patterns: overload, too much text, register-modal ambushes. Anti-reference: Slido. Both light + dark mode required.
- **F3 · Dark default;** participant + board dark-first (board high-contrast, back-row readable); light mode supported. Board appearance controls → M4 (DSN-03). **[P2 addition → T6-5]:** board sound cue seeded for M4 (DSN-05).
- **F4 · Motion explains state only;** zero decorative animation. Logo/colors greenfield → M4 kickoff [OPEN]. Hard requirement: not "yet another SaaS thing". **[P2 note]:** self-dismissal cues (announcement progress bar) qualify as state-explaining motion.
- **F5 · Invisible host:** event dominates participant surfaces; MicLine = small wordmark; landing is where MicLine speaks.

## G — Operations (P1)

- **G1 · Cost: ~€20/month busy-month target;** lockdown doubles as the cost brake; platform ceiling 1,000 participants/event. Revisit product limits after the tech phase [OPEN → post-tech/P4]. **[P2 addition → T8-10]:** M5 gains an alert-only budget threshold (notify, never auto-flip).
- **G2 · No on-call, no SLA — best effort, fix-when-awake;** boring managed stack carries the pager; maintenance banner for honesty; alerting kept simple. **[AMENDED P2 → T9-5]:** placement rules defined (terms + FAQ, phrased as what MicLine does; never landing hero; never in-product mid-event).
- **G3 · Retention numbers:** (a) event content purged **3 days** after end, moderator told explicitly; (b) accounts purged after **1 month** inactivity, no warning email; carve-outs: future event scheduled, non-empty wallet; (c) required end time + auto-end at end_time+24 h; (d) audit log 12 months; (e) magic-link tokens 15 min. Rejected: 30-day content retention; warning emails. **[AMENDED P2 → T8-6, T7-4]:** purge jobs pause during an emergency stop (overdue purges 24 h after resume); pre-purge communication is an in-product countdown, never an email.
- **G4 · All retention/limit values in central code-change-free config** (mechanism → tech interview). **[P2 additions]:** announcement lifetime, text caps, export-link validity, purge grace, curated participant-limit default, log level (FND-06).
- **G5 · Full-event UX:** clean "event is full" screen, no waitlist [PRK-04]. **Failure posture:** fail closed for creation, fail open for running events. **[AMENDED P2 → T9-2, T9-3]:** binding acceptance criteria and a canonical failure-state inventory with the state-takeover rule.
- **G6 · GDPR:** self-service deletion in M1; M5 compliance pack. Impressum + truthful privacy page from day one. **[AMENDED P2 → T9-5]:** deletion flow requires ending a running event first; deletion UI is self-sufficiently final.

## H — Locales, tone, vocabulary (P1)

- **H1 · Locales at launch: en + de.**
- **H2 · Locale mechanics.** **[PARTIALLY SUPERSEDED P2 → T3-6]:** participant surfaces now default to the *participant's device language* (not the event language); the full four-surface derivation table with persistence rules replaces the P1 wording. Emails stay in the moderator's locale.
- **H3 · German = Du by default;** `de (formal)` catalog (Sie) selectable as event language, participant surfaces + board only [ASSUMED — M2B]. Rejected: Sie everywhere; per-surface register split. **[P2 note → T3-6]:** register-variant edge (device `de` + event `de (formal)`) named for spec time; direction: the event's variant wins when bases match.
- **H4 · Register map:** landing confident/curious · participant minimal + warm micro-moments · console terse/operational · admin plain technical · emails short, zero marketing.
- **H5 · Term sheet accepted** (Einladung erforderlich; Notabschaltung / Alle Daten löschen; Mitteilung). URL scheme `/e/⟨CODE⟩`. **[P2 additions]:** Hide board / Board verbergen; "Line snapshot (HTML)" naming WIP; English documents use English terms — German only in the term sheet.

---

# P2 — Final product interview (2026-07-12)

## P2-1 — M1 scope locks & line semantics

- **T1-1 · One active waiting line item per participant (open line), fixed platform rule — new feature LINE-08.** Answered/skipped/withdrawn frees the slot; then the participant may submit again. Why: one person can't stand in a physical mic line twice; multiple positions dilute the fairness pitch on a public FIFO board; the own-question chip is singular-shaped. Rejected: multiple active items in M1 (fairness dilution, forces a "my questions" list view); per-event configurability in M1 (curated mode gets its own limit, T6-1).
- **T1-2 · Enforcement is session-scoped; device fingerprinting and IP-identity binding REJECTED, even event-scoped.** The leave-and-rejoin residual is accepted as best-effort (same stance as vote dedupe); marketing/privacy copy claims exactly "enforced per session". Why rejected: venue NAT makes IP useless at in-room events; fingerprint collisions on uniform phone fleets block innocent participants with no support path; fingerprint+IP is personal data with TDDDG-consent exposure (consent UI = the banner pillar 2 forbids) and kills the "participants: nothing to delete" privacy line; the loophole appends at the tail of a visible line and is moderator-managed per D5. Rejected alternatives: operator's event-scoped fingerprint+IP proposal (withdrawn after challenge); anonymous cookie surviving "Leave" (betrays the explicit reset action and the B6 typo escape hatch). Recorded explicitly so the tech interview cannot reintroduce tracking to close the gap.
- **T1-3 · Every line item requires written text (platform 1–280); request-to-speak REJECTED for M1 and PARKED (PRK-11).** Why: a board of "wants to speak" entries destroys the line's information value; in nickname mode it tells the room nothing; skip loses its content basis; a second item type ripples through board, review, filter, export. Zero-cost workaround: type "I'd like to comment on X". Unparking trigger: real-event demand.
- **T1-4 · Withdrawal unchanged (LINE-05);** slot frees instantly; nothing else needed under one-active-item.
- **T1-5 · No intake-closed state in M1; intake close ships M2A (MOD-05) as a manual, freely reversible toggle.** M1 lifecycle stays scheduled → live → ended (full/unknown are join screens). M1 workaround: announcement + manual end; the transparent line length self-calibrates expectations. Rejected: M1 inclusion (not essential for a first event); timers/auto-close (REJ-07 — surprise closure violates no-surprise moderation).
- **T1-6 · Per-event min/max question length within the platform 1–280 rule** — moderator creation setting, defaults = platform bounds; validation/copy details at tech interview / F001 spec. [ASSUMED M1, operator-approved.]

## P2-2 — Event information, primer, announcements, content richness

- **T2-1 · M1 primer stays pre-join-only plain text;** the announcement is the only mid-event channel in M1.
- **T2-2 · CAND-01 resolved as "reuse the primer":** in M2B the primer becomes re-openable in-event (ⓘ sheet on the participant surface), folded into STD-08. A separate information panel is REJECTED (REJ-03 — second content container, double maintenance, settings-freeze breach). Canonical division: **primer = static reference (frozen at start), announcement = live channel.**
- **T2-3 · Links render inert in all current milestones** — moderator-authored content is never auto-linked or tappable; URL-shaped text tolerated but inert. Why: zero-gate + QR entry makes MicLine an ideal quishing loader; a solo operator can't run link scanning or URL abuse triage. *Clickable links* are PARKED (PRK-09), not permanently rejected — operator explicitly kept the future option; unparking requires a dedicated security/abuse review.
- **T2-4 · No images in moderator content in current scope;** *content images* PARKED (PRK-10) with storage/purge, EXIF-privacy, and content-moderation-liability review as unparking conditions. The M2B branding logo (STD-10) is deliberately the only image in the product — narrow, single-purpose, entitlement-gated (accountable uploader).
- **T2-5 · Primer never renders on the projector board.** Announcement display model (amends B18/MOD-02): bounded lifetime (platform config value, direction ~60 s), **uniform auto-expiry on board and participant surfaces** (no stale messages on unattended machines; one mental model), subtle draining progress cue on the board [ASSUMED style — numeric countdown available on veto], clear-early action, re-send restarts the lifetime. Why uniform: split lifetimes create two mental models and stale phone banners; a stale announcement presented as current is worse than a missed one. Rejected: board-only expiry; persistent-until-replaced.
- **T2-6 · Acknowledgment modes sharpened (amends PRT-05, M1):** skippable vs required explicit confirmation; in required mode the moderator customizes the confirmation statement (plain text, capped; localized default "I understand."). Purpose: a poke to actually read — explicitly not bulletproof. Boundary derived from data minimalism: **the acknowledgment is a gate, not a record** — MicLine never stores who confirmed what; it is not legal proof of consent.
- **T2-7 · Online-use positioning clarification (amends A2 wording, not scope):** in-room = design center; online/hybrid meetings = fully supported usage context; broadcast/livestream tooling stays out of scope. "Specific in focus, deliberately versatile."

## P2-3 — Projection safety, board behavior, live-event control

- **T3-1 · Skip-stub model (amends B13):** skipping removes the question **text** from all public surfaces immediately; a minimal stub (attribution + "skipped") remains in the participant line; the projector board never shows skipped items; the owner keeps an honest chip. Why: transparency means the room sees *that* something was removed, not that the content stays amplified. Rejected: full text remains (frames the abuse); silent disappearance (violates transparent moderation).
- **T3-2 · Moderator board-hide exists, M2A (MOD-06).** Why not M1: with the stub model, skip kills amplification in seconds; first events are low-risk. Why it exists: coordinated waves mean whack-a-mole on the projector; non-abuse uses (break, alarm, sensitive moment). Pairs with intake close as the live-event-controls cluster.
- **T3-3 · Hide board is board-only.** Participant surfaces stay fully live (line, chips, withdraw, submissions); intake continues. Why: the harm is public amplification; killing phones makes the product vanish for the room; content that must leave phones is what skip is for.
- **T3-4 · Hidden board shows event title + neutral message + wordmark; no line content, no QR/code.** The message has a system default and is **moderator-customizable at hide-time** (live control per the B18 precedent — a hide-message can't be a frozen creation setting; platform max-char cap). Tech requirement logged: **the board URL must never expose the event ID or join code** (own capability token; not enumerable). Styling/animation at spec.
- **T3-5 · Naming: EN "Hide board" / DE "Board verbergen" (term sheet); explicitly distinct from the admin emergency stop.** **No audit-log entry** for hide/unhide — ops logs only (short-lived). Why: the audit log is for privileged admin/entitlement actions; no moderator in-event action keeps a durable trail (REJ-09); a 12-month trail of one moderator action would be inconsistent and privacy-hostile. [Initial audit proposal withdrawn by Claude after review; operator confirmed.]
- **T3-6 · Locale derivation model [PARTIALLY SUPERSEDES H2]:** console = moderator device default, adjustable anytime, persisted on account · event language = moderator device default at creation, adjustable before start only · participant surface = **participant device default**, adjustable anytime, persisted in session cookie/device (participants have no accounts — the "account" phrasing in the proposal was corrected) · board = event-language default, adjustable per viewer, persisted per device until event end · emails = moderator's console language. UI chrome only, never user content. Named spec-time edges: register-variant resolution (direction: event variant wins when base languages match); one person holding participant surface + board (independent persistence).

## P2-4 — M2 line modes, review, duplicates, upvotes (closes OQ2)

- **T4-1 · Curated mode public shape.** Two zones on the participant surface: the line (committed order) and the pool. **Pool visibility is a per-event creation setting:** hidden pool auto-disables upvotes; visible pool lets the moderator toggle upvotes; both freeze at start. The owner always sees their own "in pool" chip (no personal black hole). Participant pool sort: votes-desc when upvotes on, newest when off; console sorts freely. Moderator actions: place/insert, drag-reorder, return-to-pool, skip (stub semantics in both zones). Unselected items simply remain in the pool. Position stability documented as an open-line-only guarantee. Defaults [ASSUMED → M2B kickoff]: pool visible, upvotes on. Noted interplay: with a hidden pool, review approval has no participant-facing effect — review's remaining value is the explicit decline flow.
- **T4-2 · Review semantics.** Open+review: approval **appends to the line at approval time** — approval order is line order. Rejected: preserving original submission order (retro-insertion above committed items breaks position stability); manual placement in open mode. Curated+review: approval releases into the pool; line placement stays a separate curation act. Declined: owner-visible "declined" chip **with optional short moderator reason** (plain text, capped, inert, dies with purge); invisible to everyone else forever; reversible until event end; retained until purge. Rejected: silent limbo (black hole).
- **T4-3 · Duplicate merge (pool-only).** Moderator selects items, picks a **primary**; primary's text represents the group with an "×N" badge that travels with it (pool and line — it legitimizes curation, it isn't a vote count); merged items fold away publicly; owners' chips show "merged" and inherit the primary's outcome; votes combine with one-voice-per-participant dedupe (best-effort); unmerge until consumed. Stated plainly: the primary's submitter gets the mic. No merge machinery in open mode — the review tray declines duplicates there. Rejected: merging line items (strips committed positions).
- **T4-4 · Upvote boundaries.** Curated mode only, pool items only; no vote UI or counts on line items on participant surfaces/board (console may show counts); nameless in both name modes; session-deduped best-effort. Invariant verbatim: **votes advise the human, never reorder the line.** No votes in open mode (B11 stands).
- **T4-5 · No review↔co-moderator dependency.** Solo review stays possible; the creation-time consequence hint ("on = you're the bottleneck") is the guardrail. Force-rank order unchanged. Rejected: hard dependency (paternalistic, tangles ship order).

## P2-5 — Moderator console efficiency, notes, follow-up, filters

- **T5-1 · Follow-up flag: yes (MOD-08, M2A with export).** Moderator-only boolean on any item, set during event or recap window; never participant-visible, never affects public state; export column; purge-bound.
- **T5-2 · Private free-text notes: PARKED (PRK-12).** Why: hidden per-question commentary becomes a dossier about semi-identified people in real-names mode; sensitive once exportable. Flag + decline reason cover the known jobs. Trigger: concrete demand after M2.
- **T5-3 · Filters: M1 none; M2B minimal set** (pool sort, review-tray views, flagged-only). **M2B-kickoff item:** complete filter matrix across all surfaces (console, participant surface, board) incl. filter-by-submitter. Guardrail: participant/board display filters never undermine full-line transparency — the full line is the default view; filters narrow a personal display only; the board keeps one canonical layout.
- **T5-4 · Console text search (M2B):** one box over pool + line + declined, text and participant name, console-only.
- **T5-5 · Multi-select with exactly two bulk verbs: merge and bulk decline.** No bulk-approve (approval puts things in front of the room and deserves a per-item glance).

## P2-6 — Fairness indicators & participant preparation cues

- **T6-1 · Per-participant limits by mode:** open line = fixed one active item (LINE-08); curated = per-event "max open submissions per participant" (active pool+line items; answered/skipped/withdrawn/declined/merged free the slot — declined must free it or review starves submitters; merged = your voice is carried). Default/range at M2B kickoff. Why the split: in curated mode the participant-controlled act is pool submission; capping what the moderator may curate would violate ground control.
- **T6-2 · Repeat counts and submission timestamps: moderator-console-only.** Participant-visible repeat badges REJECTED (public shaming chills participation); public timestamps REJECTED (weaponizes "I was earlier" against curated mode's premise). Board shows neither. (REJ-05)
- **T6-3 · Reorder history REJECTED (not stored, not shown) and fairness warnings REJECTED** — live visible motion is the fairness mechanism; ledgers have no consumer (consistent with REJ-09); indicators inform, warnings moralize. (REJ-06) **Added scope (operator):** per-viewer motion control — participants and board viewers can collapse/expand the **pool zone** per device (PRT-09, persisted per device until event end); the line is never hideable; defaults at M2B kickoff (direction: pool collapsed on the board).
- **T6-4 · "You're up soon" ships as a third LINE-06 banner state** (approaching the top, direction: top 3) [ASSUMED M1, operator-confirmed]. **Waiting-time estimates REJECTED structurally** (REJ-04): per-question duration is variance-dominated; a wrong estimate is a broken promise from a trust product. Positions, not prophecies; the M2B speaker timer helps the moderator pace instead.
- **T6-5 · Browser push notifications REJECTED for current scope, PARKED (PRK-13)** with strict unparking conditions (evidence of tab-backgrounding + realistic iOS delivery path). Why: permission prompts contradict zero-gate posture; iOS web push needs installed PWAs; push infra is standing machinery on a solo operator. The full-screen cue remains the attention mechanism. **Added scope (operator):** board sound cue on line advance → M4 (DSN-05) [ASSUMED milestone per operator delegation]; recorded details: board-side mute, MicLine-provided sound set, browser autoplay-unlock constraint.

## P2-7 — Recap, export, data lifecycle (closes OQ5)

- **T7-1 · Recap & export raised to rank 1 and moved to M2A.** Why: every event before export ships loses content on day 3; export is small and entitlement-independent (never-gated). M1 stays export-less as decided in P1 (on-screen recap; copy states export ships next).
- **T7-2 · Formats: CSV + JSON + Line snapshot (HTML — added via T8-6), one click each, no export-time filtering — the export is everything:** questions with text, attribution, submission time, final status (answered/skipped/withdrawn/declined/merged-into), decline reasons, merge structure, vote counts, follow-up flags, primer text, stats summary. Declined items included. No announcement history (none stored). Final format list re-validated at M2A kickoff (operator note).
- **T7-3 · Export responsibility stated honestly:** the file leaves MicLine's deletion domain — the moderator's responsibility from then on; one line at export time and on the privacy page. Non-optional for real-names events.
- **T7-4 · Pre-purge communication: in-product countdown (recap + my-events), NO reminder email** (consistent with the G3b anti-spam stance; the 3-day rule is stated at creation, at event end, and on the privacy page). (REJ-08)
- **T7-5 · Participant-facing recap stays PARKED (PRK-03);** the recap UI visually separates aggregates (survive, unlinkable) from event content (dies, with countdown).

## P2-8 — Admin, entitlements, lockdown, emergency machinery

- **T8-1 · /admin gets one dedicated mandatory auth wall** (not magic-link plus a stacked factor). Mechanism (Cloudflare Access vs TOTP vs passkey) → tech interview with pricing/limits/exit-cost criteria. Hard requirement: **emergency reset always possible via Cloudflare account access alone** — no lockout dead-end. Step-up confirmation on destructive actions required regardless. [OQ1 half-closed; mechanism OPEN → tech interview / M2A admin spec.]
- **T8-2 · All P2 moderator features are never-gated** (they're moderation/safety/fairness mechanics). Gateable set unchanged. New platform values (announcement lifetime, text caps, link validity, purge grace, curated limit default) join central config (G4).
- **T8-3 · Lockdown/revocation edges:** E4/E7 verbatim; **rescheduling a committed event is never a gated act** (stewardship, not creation); **duplicate-event (STD-07) is creation** and gated under lockdown; admin god view shows the new live states (board hidden, intake closed) read-only.
- **T8-4 · Emergency-stop/delete-all split and abuse doctrine reconfirmed.** M3 note: drastic controls need a compensation & communication policy toward paying customers (BIL-01); billing brings mandated emails (BIL-02).
- **T8-5 · Rescheduling: no count cap (REJ-10).** The ±24 h anchor to original values already bounds drift (max lifetime original-end + 48 h); a once-only rule burns the venue-chaos moderator and forces delete + recreate (new join code, dead printed QR). Rescheduling joins rate-limited actions (SAFE-02). Admin-UI consequence previews and help text state these semantics.
- **T8-6 · Emergency stop = total stop (heavily amends D8/ADM-08).** Freezes events, participant surfaces, boards, console, moderator auth, creation, **registration** (P2 addition), and all scheduled jobs **including purges**. Exactly three exceptions: static notice pages, the admin UI (reversal must never require a redeploy — general rule in ADM-11), the expiring export-download endpoint (static pre-generated files, expiring tokens, no writes). Sequence: freeze instantly → generate snapshot exports for affected events (running + ended-within-purge-window) → E-4 then E-4b. **Trigger modal contains the export choice, default ON, "skip if you suspect data compromise"** (breach containment: the panic button must not exfiltrate PII in the worst case). Typed confirmation + step-up (same ceremony as delete-all). Scheduled events never open during a stop; on reversal they open late if their window is still current, else auto-end. Overdue purges execute 24 h after resume (grace, platform value); E-5 states deadlines. Participants/board see a calm emergency-ended screen — the sole participant-visible platform-initiated ending. Rejected: purge clock continuing during a stop (operator: total stop means no moving parts); purge suspension without total stop (Claude's earlier position, withdrawn).
- **T8-7 · Force-end communications:** participants/board see the normal ended state ("calm outside"); the moderator sees "ended by platform administration" + contact path + **optional admin message**, delivered in-product **and** by email (E-3) ("truthful inside"). Wording at M2A admin spec.
- **T8-8 · Board password REJECTED (reaffirmed after explicit challenge; REJ-02).** It can't protect content (participant surfaces stay zero-gate with strictly more content), it punishes the moderator on the venue machine, it opens the access-control door, and the actual threat (drive-by board access) is solved by the capability URL. Adopted instead: **regenerate board link** (MOD-07) for leak recovery. If the underlying need is ever "private events", that's a pillar-level reopen — knowingly out of scope.
- **T8-9 · Service-notice placement:** always on landing/auth/console; participant surfaces + board only when a notice is flagged as affecting live events. Details at M2A admin spec + design.
- **T8-10 · Emergency-controls inventory ratified** (20 controls across moderator/admin/operator/platform-automatic levels — see product brief). Proposal B accepted: the M5 runbook documents Cloudflare-native levers (WAF, Turnstile mode escalation) and the admin-reset procedure. Proposal C accepted: M5 alert-only budget threshold (notify, never auto-flip). **ADM-11 established:** every admin control shows a quantified consequence preview ("X events: X running, X scheduled, X in export window") with a computation timestamp ("computed at ⟨time⟩ — may already be outdated"), and defines its reverse action; irreversible parts labeled honestly.

## P2-9 — Platform email doctrine

- **T8b-1 · Complete inventory: E-1 magic link (M1) · E-2 feedback delivery (M1) · E-3 force-end notice (M2A) · E-4 emergency-stop notification (M2A) · E-4b emergency export delivery (M2A) · E-5 emergency resume (M2A).** Principles: transactional only, moderator's locale, short, zero marketing; **any new email type requires a product decision.** E-4 recipients: moderators with running, scheduled, or recently-ended (in purge window) events; E-4 prominently announces E-4b; E-4b carries all formats, ~24 h link validity (platform value), the not-the-normal-process note, and the T7-3 responsibility line.
- **T8b-2 · Deliberately no email for:** inactivity purge (G3b), content purge (T7-4), account-deletion confirmation (**the deletion UI is self-sufficiently final**: loading → explicit "all your data has been purged"; operator note), event creation/reminders, auto-end, login alerts, grant expiry/redemption (E9 in-product transparency), service notices, bans (**banned addresses silently stop receiving magic links** — anti-enumeration-consistent; no argument-inviting notification), soft-deletes (**in-product "removed by administration" state** in my-events instead), export readiness, breach notification (**M5 runbook procedure**: admin export + external send — no blast infrastructure for a hopefully-never event).
- **T8b-3 · No email-change flow in current scope (PRK-14):** delete + re-register; known pain: non-empty wallet; revisit on real demand.
- **T8b-4 · Co-moderator invitations are link-based** (capability URL shared through the moderator's own channel) — keeps the email table at its minimum; mechanism at M2B kickoff.
- **T8b-5 · Tech-interview note:** central email-template registry in the repo — every email as a template, easy text/design changes, flowing through i18n; **react.email** to be evaluated for HTML mail design.

## P2-10 — Operations, resilience, failure states

- **T9-2 · Binding acceptance criteria for the realtime engine (F003):** ① no silent loss — durably saved and visible, or explicitly reported failed; ② retries never create duplicates; ③ explicit save states (sending / saved as #N / failed, tap to retry); ④ automatic recovery — identity, own items, chips, position restore after reload/reconnect with no user action; "please refresh" is never the designed path. If an implementation can't meet one, the implementation changes, not the promise.
- **T9-3 · Canonical failure-state inventory (closed list) + tone rule:** delayed save · failed save with retry · participant "reconnecting — the line continues" banner · stale-board chip (the unattended board self-describes lag) · event full · unknown/expired code. Tone: calm, honest, no error codes or technical vocabulary on participant surfaces; exact wording at spec time. **Extensions (operator):** device-agnostic wording ("participant surface", not phone); **state-takeover rule** — races with state changes resolve into the new canonical state with calm feedback, never raw errors (ended/force-ended/emergency/intake-closed/full); **moderator-console state set** — reconnecting, action-failed-retry, admin-ended notice, emergency notice.
- **T9-4 · Degraded modes are M5-only, designed after load-test evidence** (CAND-10). M1/M2 ship reconnect + stale states only. **Metrics visibility ladder:** participants none, ever · moderators in-event product stats only (live participant count) · admin metrics wall (D6) · raw infrastructure operator-only.
- **T9-5 · No-SLA placement and sacred-events copy rules:** best-effort/no-SLA lives in terms + FAQ, phrased as what MicLine does; never on the landing hero; never in-product mid-event. Failure copy always frames recovery, never termination. The emergency-ended screen is the sole participant-visible platform-initiated ending. **GDPR carve-out resolution (operator question):** the 3-day purge is *not* a carve-out (acts on ended events); deletion paths are — **self-service deletion requires ending a running event first** and shows a quantified consequence preview of scheduled events; admin deletions ride force-end semantics; delete-all-data is operationally preceded by emergency stop. Participants never see "GDPR" internals.

## P2-11 — Milestones, process, terminology

- **T10-1 · M2 split into M2A "Control plane & data" and M2B "Stand-out modes" [supersedes the single-M2 plan].** Execution order: **M1 → M2A → M2B → M4 → M5 → M3-if-ever.** M2A: entitlement engine → recap & export (emergency machinery consumes it) → admin UI incl. full emergency model → invite codes + lockdown; live-safety controls (intake close, hide board, regenerate board link) and follow-up flag slot in anywhere. M2B: force-rank curated → upvotes → review → co-mods (+ filters/search/bulk riding along) → timer → templates → rich content → vanity → branding (executes after M4); parallel: TMS, de (formal). Why the split: M2 was carrying two milestones (platform control vs product modes); each half is a shippable increment — after M2A the platform is publicly defensible (cost brake, abuse tools, emergency machinery, data honesty), after M2B it stands out. Rejected: pushing features to M4/M5 (would corrupt their identities: M4 = presentation only, M5 = longevity); renumbering M3/M4/M5 (doc churn).
- **T10-2 · Tech foundation is F000/build-guide scope, not a product milestone (no M0).** Binding tech-interview requirement: fully and precisely outline the Cloudflare + GitHub (Actions) setup as the first implementation topic; revisit and adjust the foundation once a bare-minimum deployable product exists.
- **T10-3 · Terminology rule:** English documents use English terms ("announcement", "the line"); German appears only in the term sheet's DE column.
- **T10-4 · Global log-level control (FND-06, M1):** deployment-wide, debug ↔ critical, freely operator-adjustable; details at tech interview.
- **T10-5 · Quality gates passed at freeze:** no presentation/session model; no participant accounts or access gates (zero-gate intact); no M0/M6; M1 unchanged in surface count; M2 semantics precise; 3-day retention intact; aggregates separate; open questions minimal with owners.

---

## Open questions & scheduled decisions (consolidated, post-P2)

### Genuinely open (few, with owner + consequence)

| # | Question | Owner / when | Consequence if unresolved |
|---|---|---|---|
| OQ1 | Admin-wall mechanism: Cloudflare Access vs TOTP vs passkey (criteria: pricing, limits, exit cost; CF-account reset mandatory) | Tech interview / M2A admin spec | /admin ships without its dedicated wall — blocked |
| OQ6 | Revisit product limits (participants, events, ceiling) against measured cost & purpose | After tech phase / P4 | Limits stay at P1 guesses |
| OQ7 | Magic-link auth flow details (operator wants depth) | F002 spec/clarify | Auth spec incomplete |
| OQ8 | Central platform-config + feature-flag mechanism | Tech interview | Config values have no home |
| OQ9 | Logo & color identity | M4 kickoff | Wordmark placeholder persists |
| OQ10 | Admin UI v2 features | Parked (PRK-08) | None |

Closed in P2: **OQ2** (review semantics → T4-2) · **OQ5** (export rank → T7-1). Halved: **OQ1** (wall decided; mechanism open). Unchanged by design: OQ3 (invite-code UX → M2A kickoff) · OQ4 (TMS → M2B kickoff).

### Scheduled decisions (owned, dated — not open questions)

- **M2A kickoff:** invite-code UX details (OQ3: format, QR sharing, custom message, redemption display, generation UX, clock defaults) · final export-format list · emergency-copy wording set (E-4/E-4b/E-5, force-end notices, emergency-ended screen).
- **M2B kickoff:** cross-surface filter matrix (incl. filter-by-submitter; transparency guardrail applies) · curated defaults (pool visible, upvotes on — assumed) · per-participant submission limit default/range · markdown-lite subset · pool-collapse defaults (direction: collapsed on board) · co-moderator link-invite mechanism · TMS replaces-or-feeds Lunaria (OQ4).
- **M4 kickoff:** logo & colors (OQ9) · board sound cue build decision (DSN-05).
- **Tech interview (binding inputs):** complete Cloudflare + GitHub setup outline first (T10-2) · admin-wall mechanism + CF-reset (OQ1) · board capability-URL token design (no event ID/join code, no enumeration) · config/feature-flag mechanism (OQ8) · log-level implementation (FND-06) · email-template registry (react.email evaluation) · magic-link depth (OQ7) · rate-limit keys incl. rescheduling · emergency-stop download-endpoint isolation.
- **Spec time:** exact failure-state and emergency copy · confirmation-statement/hide-message/decline-reason caps · announcement lifetime value · register-variant edge (`de` device + `de (formal)` event) · dual-surface viewer edge · board autoplay-unlock for the sound cue (M4) · intake-close display details · review verb wording (approve/decline) + DE terms.
